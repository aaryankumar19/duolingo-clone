{
  "name": "duolingo-clone-root",
  "private": true,
  "scripts": {
    "dev": "bun --cwd frontend run dev",
    "build": "bun --cwd frontend run build",
    "start": "bun --cwd frontend run start"
  }
}
